---
layout: default
---

<div class="container">
    <h1>posts</h1> 
    <ul>
      {% for post in site.posts %}
        <li>
            [{{post.date | date : "%F"}}]<a href="{{ post.url }}">{{ post.title }}</a>
        </li>
      {% endfor %}
    </ul>
</div>
